<Window x:Class="CMS22_Assignment2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CMS22_Assignment2"
        mc:Ignorable="d"
        Title="OrderManager" Height="800" Width="550">
    <Grid Margin="20">
        <Grid x:Name="OrderView">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="9*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            
            <StackPanel Grid.Row="0">
                <TextBlock Text="Kund" FontSize="15"/>
                <ComboBox x:Name="cb_Customer" Margin="0 5 0 20"/>
                <TextBlock Text="Produkter" FontSize="15"/>
                <ComboBox x:Name="cb_Products" Margin="0 5 0 20"/>
            </StackPanel>

            <ListViewItem Grid.Row="1" x:Name="lv_OrderRows" Margin="0 20 0 0">
                
            </ListViewItem>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="bt_Add" Click="bt_Add_Click" Content="Lägg till" Margin="0 10 15 10" HorizontalAlignment="Right" Padding="5"/>
                <Button x:Name="bt_PutOrder" Click="bt_PutOrder_Click" Content="Skapa Order" Margin="0 10 0 10" Padding="5"/>
            </StackPanel>
            
        </Grid>
    </Grid>
</Window>
